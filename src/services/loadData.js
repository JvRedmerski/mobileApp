import {HeaderMock} from '../Mocks/header'
import {productorMock} from "../Mocks/productor";

export const loadHeader = () => {
    return HeaderMock
}

export const loadProducts = () => {
    return productorMock
}